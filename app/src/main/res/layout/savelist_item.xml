<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.example.new_application.retrofit.response.Article" />

        <variable
            name="saveArticle"
            type="Article" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/saveList_item_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        >


        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/saveImage_list"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            app:layout_constraintTop_toTopOf="parent"

            />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/text_view_list_item"
            style="@style/TextAppearance.AppCompat.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/saveImage_list"
            android:padding="8dp"
            android:text="@{saveArticle.title}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/saveImage_list"
            />



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>